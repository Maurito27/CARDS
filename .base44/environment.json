{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "JWT_SECRET",
      "description": "Secret key for signing JWT authentication tokens. Generated for development; replace with a strong random value for production.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    }
  ]
}
